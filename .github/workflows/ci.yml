name: CI

on:
  push:
    branches:
      - main
      - "pull-request/[0-9]+"
    tags:
      - '**'

jobs:
  lint:
    runs-on: linux-amd64-cpu8
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup uv
        uses: astral-sh/setup-uv@v6

      - name: Install dependencies
        run: |
          # ty needs third-party packages to resolve imports. Skip packages
          # that cannot be installed on CPU-only runners:
          # - transformer-engine-torch: source-only, requires CUDA to compile
          uv sync --extra dev --group lint \
            --no-install-package transformer-engine-torch

      - name: Run linter checks
        run: uv run --no-sync pre-commit run -a

  cpu:
    runs-on: linux-amd64-cpu8
    steps:
      - name: Get PR info
        if: startsWith(github.ref_name, 'pull-request/')
        id: get-pr-info
        uses: nv-gha-runners/get-pr-info@main

      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup proxy cache
        uses: nv-gha-runners/setup-proxy-cache@main

      - name: Setup uv
        uses: astral-sh/setup-uv@v6

      - name: Build flashdreams wheel
        run: |
          uv run .github/scripts/sync_version.py || true
          uv build --wheel --package flashdreams

      # Documentation is built and deployed by `.github/workflows/doc.yml`.

  gpu:
    runs-on: linux-amd64-gpu-rtxpro6000-latest-2
    steps:
      - name: Get PR info
        if: startsWith(github.ref_name, 'pull-request/')
        id: get-pr-info
        uses: nv-gha-runners/get-pr-info@main

      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup proxy cache
        uses: nv-gha-runners/setup-proxy-cache@main

      - name: Verify GPU availability
        run: |
          echo "Verifying GPU access..."
          nvidia-smi

      - name: Run world model diffusion
        run: |
          echo "Running world model diffusion workloads..."
          # Add your world model diffusion commands here

  # ===========================================================================
  # Publish to Test PyPI -- runs only on main after lint passes.
  # TEMPORARY: targets test.pypi.org while the project is pre-release.
  # Switch --publish-url/--check-url to the real PyPI endpoints and the
  # secret to PYPI_API_TOKEN when going public.
  # ===========================================================================
  publish-testpypi:
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    needs: [lint]
    runs-on: linux-amd64-cpu8
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup uv
        uses: astral-sh/setup-uv@v6

      - name: Sync versions
        run: uv run .github/scripts/sync_version.py || true

      - name: Build wheel
        run: uv build --wheel --package flashdreams

      - name: Publish to Test PyPI
        # uv publish exits non-zero if the version already exists on the
        # index (PyPI returns HTTP 400).  The "|| true" makes this
        # non-fatal so unchanged versions don't break the build.
        # NOTE: --check-url is intentionally omitted -- it requires exact
        # hash match with the remote file, which fails when the wheel was
        # previously uploaded from a different build (different metadata
        # timestamps).  Letting PyPI reject duplicates is sufficient.
        run: >
          uv publish
          --publish-url https://test.pypi.org/legacy/
          --token ${{ secrets.TEST_PYPI_API_TOKEN }}
          dist/*
          || true
